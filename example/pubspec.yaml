name: signal_strength_indicator_example
description: Signal strength indicator widget example.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  signal_strength_indicator:
    path: ../

dev_dependencies:
  lint: ^1.6.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  uses-material-design: true

